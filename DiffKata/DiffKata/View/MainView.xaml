<Window x:Class="DiffKata.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DiffKata.View"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="800"
        xmlns:dd="clr-namespace:DiffKata.DesignData"
        d:DataContext="{d:DesignInstance {x:Type dd:KataViewDesignModel}, IsDesignTimeCreatable=True}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <ListView Grid.Column="0" Name="HeadList" ItemsSource="{Binding HeadlistValues}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <DockPanel >
                        <TextBlock Text="Hallo">
                            
                        </TextBlock>
                    </DockPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <ListView Grid.Column="1" Name="BranchList" ItemsSource="{Binding BranchlistValues}">


        </ListView>


    </Grid>
</Window>
